diff --git a/src/pkg/runtime/debug.go b/src/pkg/runtime/debug.go
index b802fc6..1bcf934 100644
--- a/src/pkg/runtime/debug.go
+++ b/src/pkg/runtime/debug.go
@@ -32,6 +32,9 @@ func NumCgoCall() int64
 // NumGoroutine returns the number of goroutines that currently exist.
 func NumGoroutine() int
 
+// NumGoroutine returns the number of runnable (not blocked) goroutines that currently exist.
+func NumRunnableGoroutine() int
+
 // MemProfileRate controls the fraction of memory allocations
 // that are recorded and reported in the memory profile.
 // The profiler aims to sample an average of
diff --git a/src/pkg/runtime/proc.c b/src/pkg/runtime/proc.c
index 04a9926..3902ab4 100644
--- a/src/pkg/runtime/proc.c
+++ b/src/pkg/runtime/proc.c
@@ -1703,6 +1703,13 @@ runtime·NumGoroutine(int32 ret)
 	FLUSH(&ret);
 }
 
+void
+runtime·NumRunnableGoroutine(int32 ret)
+{
+	ret = runtime·sched.grunning;
+	FLUSH(&ret);
+}
+
 int32
 runtime·gcount(void)
 {
